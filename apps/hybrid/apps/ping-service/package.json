{
  "name": "@archkit/ping-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && echo 'built' > dist/out.txt",
    "start:dev": "echo 'pinging'",
    "lint": "echo 'linted'",
    "test": "echo 'tested'"
  }
}
